# SearchFilterBar

## Vue target

```text
components/ui/SearchFilterBar.vue
```

## Structure

```text
SearchFilterBar
├── Select optional
├── Select optional
├── KeywordInput
└── SearchButton
```

## Behavior

검색 버튼 클릭 시 현재 route에 query를 반영한다.

```text
?searchType={type}&keyword={keyword}&page=1
```
